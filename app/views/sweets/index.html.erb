<h1> 🍪 Sweets Index 🍪 </h1>

<h3>Select a Vendor to view their Sweets:</h3>

<ul>
  <% @sweets.each do |sweet| %>
  <% @vendor_sweet = sweet.vendor_sweets.find { |vs| vs.sweet_id == sweet.id} %>
    <li><%= link_to sweet.name, sweet_path(sweet) %> at
        <%= @vendor_sweet.price %>¢
    </li>
  <% end  %>
</ul>
