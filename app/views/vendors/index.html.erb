<%= link_to "Sweets Index", sweets_path %>
<%= link_to "Create VendorsSweets", vendor_sweets_path %>

<h1>All Vendors</h1>

<ul>
  <% @vendors.each do |vendor| %>
    <li><%= link_to vendor.name, vendor_path(vendor) %></li>
  <% end  %>
</ul>
