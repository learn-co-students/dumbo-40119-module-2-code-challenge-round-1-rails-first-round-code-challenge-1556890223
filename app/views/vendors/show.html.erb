<h1><%= @vendor.name  %> </h1>

<%= button_to "Add Sweet", new_vendor_sweet_path, :method => :get %>

<h3>Our Delicious Sweets:</h3>

<ul>
  <% @vendor.sweets.each do |sweet| %>
    <li>
     <%= link_to sweet.name, sweet_path(sweet) %>
    </li>
    <% end %>
</ul>
